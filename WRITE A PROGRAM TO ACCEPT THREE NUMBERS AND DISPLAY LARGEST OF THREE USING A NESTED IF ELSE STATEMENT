#include <stdio.h>
int main() {
int a, b, c, max;
printf("Enter Number A = ");
scanf("%d", &a);
printf("Enter Number B = ");
scanf("%d", &b);
printf("Enter Number C = ");
scanf("%d", &c);
max = a;
if (b > max)
max = b;
if (c > max)
max = c;
printf("%d is the largest number.\n", max);
return 0;
}
